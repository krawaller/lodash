define(['./function/after', './function/ary', './function/before', './function/bind', './function/bindKey', './function/curry', './function/curryRight', './function/debounce', './function/defer', './function/delay', './function/flip', './function/memoize', './function/negate', './function/once', './function/overArgs', './function/partial', './function/partialRight', './function/rearg', './function/rest', './function/spread', './function/throttle', './function/unary', './function/wrap'], function(after, ary, before, bind, bindKey, curry, curryRight, debounce, defer, delay, flip, memoize, negate, once, overArgs, partial, partialRight, rearg, rest, spread, throttle, unary, wrap) {
  return {
    'after': after,
    'ary': ary,
    'before': before,
    'bind': bind,
    'bindKey': bindKey,
    'curry': curry,
    'curryRight': curryRight,
    'debounce': debounce,
    'defer': defer,
    'delay': delay,
    'flip': flip,
    'memoize': memoize,
    'negate': negate,
    'once': once,
    'overArgs': overArgs,
    'partial': partial,
    'partialRight': partialRight,
    'rearg': rearg,
    'rest': rest,
    'spread': spread,
    'throttle': throttle,
    'unary': unary,
    'wrap': wrap
  };
});
