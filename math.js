define(['./math/add', './math/ceil', './math/floor', './math/max', './math/maxBy', './math/mean', './math/min', './math/minBy', './math/round', './math/subtract', './math/sum', './math/sumBy'], function(add, ceil, floor, max, maxBy, mean, min, minBy, round, subtract, sum, sumBy) {
  return {
    'add': add,
    'ceil': ceil,
    'floor': floor,
    'max': max,
    'maxBy': maxBy,
    'mean': mean,
    'min': min,
    'minBy': minBy,
    'round': round,
    'subtract': subtract,
    'sum': sum,
    'sumBy': sumBy
  };
});
